# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
# default: &default
#   adapter: sqlite3
#   pool: 5
#   timeout: 5000
#
# development:
#   <<: *default
#   database: db/development.sqlite3

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
# test:
#   <<: *default
#   database: db/test.sqlite3
#
# production:
#   <<: *default
#   database: db/production.sqlite3

defaults: &defaults
  adapter: mysql
  username: root
  password: 0gz3Zhpd
  host: localhost

development:
  <<: *defaults
  database: zwitter_development

test:
  <<: *defaults
  database: zwitter_test


  #For MySQL
  # development:
  # adapter: mysql2
  # database: db_name_dev
  # username: koploper
  # password:
  # host: localhost
  # socket: /tmp/mysql.sock
